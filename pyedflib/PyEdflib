import pyedflib
import numpy as np
import os

file_name = pyedflib.data.test_generator()
n = file_name.signals_in_file
signal_labels = file_name.getSignalLabels()
sigbufs = np.zeros((n, file_name.getNSamples()[0]))
for i in np.arange(n):
    sigbufs[i, :] = file_name.readSignal(i)
